import styled from "styled-components";

const StyledNotesMap = styled.div`
  background-color: green;
  padding: 4rem;
`;

function NotesMap() {
  return <StyledNotesMap>NOTES MAP</StyledNotesMap>;
}

export default NotesMap;
